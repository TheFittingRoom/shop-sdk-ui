import { WidgetProps } from '@/lib/views'
// import { useMainStore } from '@/lib/store'

export default function SizeRecWidget({}: WidgetProps) {
  return <div>This is the size recommendation widget.</div>
}
